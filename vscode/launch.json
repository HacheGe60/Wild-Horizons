{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Debug en Chrome (abre DevTools)",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "devtools": true
        }
    ]
}